<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="jar" name="Create jar for current project">
    <target name="src-jar" description="Create a source jar for this project">
        <jar destfile="grading.jar">
            <fileset dir="${basedir}">
            	<include name="**/*.java"/>
            	<include name="**/*.class"/>
        	</fileset>
        </jar>
    </target>
	<target name="jar" description="Create a jar for this project">
        <jar destfile="grading.jar">
            <fileset dir="${basedir}">
            	<include name="**/*.class"/>
        	</fileset>
        </jar>
        <jar destfile="D:\dev\lib\IDC-417\5771\grading.jar">
            <fileset dir="${basedir}">
            	<include name="**/*.class"/>
        	</fileset>
        </jar>
    </target>
</project>
